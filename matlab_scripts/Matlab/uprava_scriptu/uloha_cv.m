% clc; clear all; close all; echo off;
% 
% key = 'matlab';
% text = 'AHOJJAJSEMTVUJSUPERPOMOCNIK';
% 
% text_tmp = zeros(1,ceil(length(text)/length(key))*length(key));
% text_tmp(1:length(text)) = text;
% text = char(text_tmp);
% 
% [c cisla] = sort(double(key));
% 
% M = reshape(text,length(key),ceil(length(text)/length(key)))';
% out = reshape(M(:,cisla),1,length(text))

6!